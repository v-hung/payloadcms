# Secret key for Payload CMS encryption - keep this secure and change in production
PAYLOAD_SECRET=your-secret-key-here
# SQLite database file path - data will be stored locally
DATABASE_URL=file:./database/sqlite.db
# Authentication token - only required for remote databases (Turso, LibSQL Cloud), leave empty for SQLite
DATABASE_AUTH_TOKEN=